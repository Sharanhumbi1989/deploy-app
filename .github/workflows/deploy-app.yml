name: deploy-app
on:
   push:
       braches:
         - main
jobs:
   build:
     runs-on: ubuntu-latest
     steps:
         - name: Checkout code
           uses: actions/checkout@v4

         - name: Deploy
           run: echo "Deployment success"     

